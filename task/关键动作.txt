读取工单、接管与离线规则，确认工单和场景之间的关联

启动只提供工作台页面和工单fixture的本地HTTP服务

为每条接管记录创建独立browser context和两个共享存储的page

在旧标签填写草稿并刷新，从textarea读取恢复结果

由第二个标签发布storage消息，在旧标签观察事件、提示与发送按钮状态

在另一context中切换离线、连接中断、网络恢复和排队消息送达

从浏览器观察生成接管矩阵、草稿恢复和网络状态三份报告
